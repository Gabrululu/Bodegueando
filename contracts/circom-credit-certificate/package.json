{
  "name": "circom-credit-certificate",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "compile": "circom circuits/creditCertificate.circom --r1cs --wasm --sym -o build -l node_modules/circomlib/circuits",
    "test": "node test/creditCertificate.test.mjs"
  },
  "dependencies": {
    "circomlib": "^2.0.5",
    "circomlibjs": "^0.1.7",
    "snarkjs": "^0.7.5"
  },
  "devDependencies": {
    "circom_tester": "^0.0.20"
  }
}
